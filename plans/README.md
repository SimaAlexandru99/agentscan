# Implementation Plans

Plans 001–003 generated by the `improve` skill on 2026-08-09 against `e1c9976`;
plans 004–008 added the same day by `/improve deep` against `8e6098d`.

Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update your
row when done.

Neither run had a user select findings interactively, so both wrote the top
items by leverage and recorded the default here, per the skill's non-interactive
rule.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 004 | `bun test` and typecheck run automatically on every push | P1 | S | — | DONE `50686c8` |
| 005 | A malformed project file produces a finding, never a dead scan | P1 | S | 004 | DONE `5d8db3b` |
| 006 | Scanned content cannot forge report lines or drive the terminal | P1 | S | 004 | DONE `3a7697b` |
| 007 | `--global` and `includeGlobal` produce output a user can act on | P2 | S | 004 | DONE `0d89fd2` |
| 008 | Findings say true things — parser and message defects | P2 | M | 004 | DONE `3430c56` |
| 001 | Report skill files pointing at bundled files that do not exist | P1 | M | 004 | DONE |
| 002 | Report skills an agent cannot tell apart | P2 | S | 004 | DONE |
| 003 | Validate agent definition files instead of only counting them | P3 | S | 008 ✓ | DONE (2 of 4 checks; two cut) |

| 009 | Fix what three independent reviews found | P0 | M | — | DONE `a7a4b78` |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale)

## Dependency notes

- **004 comes first and everything depends on it.** The deep audit found nine
  defects a running test suite would not have caught by itself, but every fix
  below is verified by `bun run typecheck` and `bun test`, and nothing runs them
  today. Until 004 lands, "green" is a claim.
- **003 now depends on 008.** Step A of 008 fixes the frontmatter regexes that
  003 planned to generalise for agent files; doing 003 first means generalising
  a broken parser.
- **005, 006, 007, 008 are independent of each other** and of 001/002. They can
  run in parallel after 004.
- Shared collision points if run concurrently: 001, 002 and 003 all append to
  `STRUCTURAL_CHECKS` (`src/checks/index.ts:23`) and to the sync-test fixture;
  005 and 008 both touch `src/discover/index.ts`. Conflicts there are mechanical.
- **001 and 002 were re-prioritised below 004–006.** They add new detection;
  004–006 fix the tool being wrong or dying. Correctness before coverage.

## Why 004–008 exist: the audit behind them

`/improve deep` ran six parallel read-only auditors over all nine playbook
categories, then every finding below was re-verified by opening the cited code
and reproducing the behaviour. Roughly 68 raw findings came back; these are the
ones that survived.

The pattern worth naming: **most of them are the exact failure class the tool
sells.** A corrupt `package.json`, a BOM, a typo'd config key, a broken builtin
rule — each produces `Summary: no findings` or a confidently false message. The
tool did not apply its own standard to itself.

Reproduced defects, all confirmed by hand:

| Defect | Plan |
|---|---|
| `{"scripts":{"build":null}}` kills the whole scan with a raw TypeError | 005 |
| A skill directory named with `\n` forges complete report lines | 006 |
| ANSI escapes from a scanned repo reach the operator's terminal | 006 |
| `includeGlobal: true` in config is silently discarded (0 vs 22 skills) | 007 |
| `--global` reports 22 home-directory skills as "not in the project lockfile" | 007 |
| Empty `name:` captures the next line, emitting a nonsense mismatch | 008 |
| A UTF-8 BOM reports `skill.missing-frontmatter` on valid frontmatter | 008 |
| Anthropic keys are labelled "OpenAI-style key" — wrong provider to rotate | 008 |
| `$&` in a skill name corrupts the finding id, breaking `explain` | 008 |
| A 200-line `AGENTS.md` is reported as 201 lines | 008 |
| `hasConfig: "constructor"` matches every project via `Object.prototype` | 008 |
| `.gitkeep` and `README.md` count as agents | 008 |
| `bun.lock` still says `skillscan` after the rename | 008 |
| No CI; nothing runs the suite | 004 |

## What the spec research changed

After these plans were written, the published docs were read for every
assumption the checks encode. Four of five were wrong, and they produced 25 of
the 37 findings the tool reported across 17 projects:

- The hook-event list had 9 names; the spec has 31. `PostToolBatch` was reported
  as a dead hook at severity error. **Fixed.**
- `skill.name-mismatch` enforced a rule the spec contradicts — `name` is
  optional and defaults to the directory. 24 false findings. **Deleted.**
- `skill.missing-name` enforced an optional field. **Deleted.**
- `budget.skills` counted skills; the real constraint is the byte budget shared
  by all descriptions at startup. **Replaced** by `skill.description-budget`.
- `AGENTS.md > 150` and `CLAUDE.md > 200` both held up, and their reasons now
  cite the mechanism rather than asserting a number.

Plan 003 was re-scoped as a direct consequence: its `agent.unknown-model` and
`agent.unknown-tool` checks are the same shape as the hook list that failed, so
they are cut until someone can cite a complete enumeration.

The rule this leaves behind: **a check is written against a published spec line,
never against what happens to appear in real projects.**

## Findings considered and rejected

From the deep audit:

- **Unbounded loop in `resolveRoot`.** Reported as a hang risk; it is bounded by
  the filesystem root. Not a defect.
- **`McpFact.raw` retaining credential text.** Structurally untidy, but tested
  from four angles — a secret in MCP `env`, in MCP `args`, in `AGENTS.md`, and in
  a parser error `detail` — and it never reaches stdout or JSON in any of them.
  Latent, not live; fold into a future refactor rather than its own plan.
- **Finding-id collisions from crafted names.** Attempted to construct one with
  `:` inside a hook event name; ids stayed unique. Not confirmed.
- **`localeCompare` in `sortFindings` making output locale-dependent.** Measured:
  Bun resolves to `en-US` regardless of `LC_ALL`/`LANG` on this build, so output
  does not vary. A one-line hardening, not a bug.
- **Micro-optimisations**: `allDeps()` re-merging per clause, `statSync` per
  directory entry, `sortFindings` running three times. Measured as irrelevant at
  2000 skills in 0.07s.
- **A percentage score.** Measured across 17 real projects: every one scores
  97.7–100%, and the score *inverts* severity because config defects do not
  scale with config size. A severity verdict (`BROKEN`/`DEGRADED`/`CLEAN`) would
  discriminate — 2/9/6 across the fleet — but that is a product decision, not a
  defect, and it is not planned here.

Carried forward from the first run, still rejected:

- **Content drift via `computedHash`.** The `skills` CLI ships `skills update`,
  which owns this. Confirmed again: the hash is not reproducible from
  `SKILL.md` bytes (0/26 match on a pristine install).
- **Matching an agent's frontmatter `name` to its filename.** 16 of 34 real
  files differ intentionally (`name: API Platform Engineer`). Plan 003 carries a
  regression test against re-adding it.
- **Reviving `DEP_SKILL_MAP`.** Deleted after measuring zero findings across 17
  projects.

## Open decisions for the maintainer (not planned)

These came out of the audit with real evidence but need a product call, so no
plan was written:

- **Drop `yaml` for `Bun.YAML.parse`.** Verified: the native API exists and
  produces byte-identical output on all six shipped rules; the import costs 20ms
  of a 50ms run. The catch is `engines.bun` must move from `>=1.1.0` to
  `>=1.2.21`, and user-authored rules are the reason a full YAML parser is there.
- **`zod ^3.24.0` resolves to the v3/v4 bridge**, shipping 2.2 MB of unreachable
  v4 alongside the v3 API in use. Migrating is a two-file change.
- **~90 lines of Next.js config parsing** (`src/facts/extract.ts:92-179`) feed
  `Facts.configs.next`, which the documented flat-key `hasConfig` matcher cannot
  reach. Either make `hasConfig` support dotted paths or delete the parsing.
- **The YAML rule engine's fate.** Still unresolved from the first run, and the
  deep audit sharpened it: no test fires a builtin rule, no builtin rule uses
  `dep`/`skillMatches`/`hasConfig`, and a typo'd clause is indistinguishable from
  a correct non-matching one. Either invest (strict `when` schema, the missing
  `any`/`hasMcp`/`hasHook` matchers) or shrink it.
- **No `AGENTS.md` in the repo whose product audits `AGENTS.md`.** Three
  agent-executable plans are queued here and the context they need is scattered
  across README, a code comment, and this file.
