# Roo Code skills

**Source:** https://docs.roocode.com/features/skills
**Read:** 2026-09-07
**Depends on it:** `agent-skills.skill.*` on `.roo/skills`

Quoted locations: `.roo/skills/` (project), `~/.roo/skills/` (global).
Also `~/.agents/skills/` (already scanned as portable Agent Skills).

Quoted: `name` and `description` are required; descriptions 1–1024 characters.

Project skills override global skills. This scanner inventories both.

## Staleness risk: MEDIUM
